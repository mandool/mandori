name: Keep Site Awake

on:
  schedule:
    # 한국 시간 기준 매일 오전 9시 실행 (UTC 0시)
    - cron: "0 0 * * *"
  workflow_dispatch: # 수동 실행 버튼

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping target server
        run: curl https://mandori.vercel.app/
